import React from 'react';

import { SettingsOptions } from '../../features/settings';

export const SettingsPage: React.FunctionComponent = () => {
  return (
    <React.Fragment>
      <SettingsOptions/>
    </React.Fragment>
  )
};